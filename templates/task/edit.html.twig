{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col col-md-6 col-md-offset-3">
        {{ form_start(form, {'action' : path('task_edit', { 'id' : task.id })}) }}
            {{ form_widget(form) }}

            <button type="submit" name="task_edit_submit" class="btn btn-success pull-right">Modifier</button>
        {{ form_end(form) }}
    </div>
    </div>
{% endblock %}
